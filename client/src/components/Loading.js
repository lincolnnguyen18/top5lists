const Loading = () => {
  return (
    <div style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      flexDirection: 'column',
      backgroundColor: '#fff',
      padding: '3em',
      width: 'fit-content',
      margin: 'auto',
      height: '100%',
    }}>
      <h1>Loading...</h1>
    </div>
  );
}
export default Loading;